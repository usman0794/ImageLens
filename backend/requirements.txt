# ===== Web framework =====
fastapi==0.137.1
uvicorn[standard]==0.49.0
python-multipart==0.0.32
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.1

# ===== MongoDB =====
motor==3.7.1
pymongo==4.17.0
dnspython==2.8.0

# ===== AWS S3 =====
boto3==1.43.31

# ===== Vector database (Qdrant Cloud) + image handling =====
# FAISS removed: vectors now live in Qdrant Cloud (persistent), so the index
# survives Render restarts/redeploys.
qdrant-client
numpy
pillow

# ===== Remote CLIP service (HF Space) =====
# CLIP runs on a separate Hugging Face Space; the backend only makes HTTP calls.
requests
